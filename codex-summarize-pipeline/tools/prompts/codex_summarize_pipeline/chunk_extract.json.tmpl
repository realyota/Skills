You are reading a single chunk from a larger article.
Do NOT run any commands or tools.
Do NOT add filler. Be information-dense.
If something is uncertain, say so briefly.

CHUNK $chunk_index/$chunk_count:
-----
$chunk_text
-----

Task: Extract a compact JSON object capturing only durable facts.
Output rules:
- Output MUST be valid JSON and NOTHING ELSE.
- Keep it small.
- Use these keys: claims (array), numbers (array), timeline (array), entities (array), open_questions (array).
- Each array item should be a short string.

